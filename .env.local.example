# Copy this file to .env.local and fill in your key.
# Optional: only needed for the Gemini-powered AI tuning advisor.
# Without it, the app still runs and uses the local heuristic advisor.
GEMINI_API_KEY=
